# Admin credentials (optional — defaults shown below)
USERNAME=admin
PASSWORD=monitor2025!

# JWT signing secret (required — use a random string ~30 characters)
JWT_SECRET=

# Comma-separated list of allowed CORS origins (optional)
# ALLOWED_ORIGINS=https://example.com,https://app.example.com
